name: Approve PR Sync
on: 
  pull_request_target:
    branches:
      - 'int/**'
      
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    if:  startsWith(github.event.pull_request.head.ref, 'sync/')
    steps:
      - name: Initial
        id: node_version
        run: echo "start ..."
      - name: Debug
        run: echo "Branch name:${{ github.event.pull_request.head.ref }}"
