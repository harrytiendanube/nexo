name: Approve PR Sync
on:
  pull_request_target:
    types:
      - opened
    branches:
      - 'sync/**'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: hmarr/auto-approve-action@v3
